import React from 'react';
import { SideBar } from '../components';
import { ProductList } from '../components';
const Main = () => {
    return (
        <>
            <SideBar/>
            <ProductList/>
        </>
    );
}

export default Main;
